// GoldenPie — Spearmint (Quake III) gamepad setup for up to 4 splitscreen players.
//
// This file holds only the STATIC per-player device cvars. Two things are GENERATED by the
// adapter (adapters/spearmint-log.js) and spliced into the deployed copy at launch:
//   1. the tunable "feel" cvars — look speed + stick deadzone, driven by the in-app
//      Settings -> Controller (Quake III) page — inserted at the @goldenpie:tuning marker below;
//   2. the key BINDS, appended at the end.
// Binds are generated because they need per-player command prefixes that are easy to get wrong:
//   Spearmint decides WHICH player a command moves from the COMMAND NAME, not the key. So
//   player 1 uses "+forward", player 2 uses "+2forward", player 3 "+3forward", player 4 "+4forward"
//   (the number goes right after the +/-; plain commands like weapnext become 2weapnext, etc.).
//   Binding player 2's keys to the UNPREFIXED command makes player 2's controller move player 1.
// The adapter also applies the macOS stick-Y inversion there, per player.

// --- Enable a joystick for each player, pointed at a distinct device (SDL enum order) ---
seta in_joystick "1"
seta 2in_joystick "1"
seta 3in_joystick "1"
seta 4in_joystick "1"

seta in_joystickNo "0"
seta 2in_joystickNo "1"
seta 3in_joystickNo "2"
seta 4in_joystickNo "3"

// Analog stick mode: proportional look/move (push the stick further = turn faster) instead of
// fixed-speed digital look, which felt sluggish. This STILL routes per-player: the engine applies
// each axis's analog magnitude to the local player named by its bound command, and our binds are
// per-player prefixed (+2forward, +2left, +2lookup, ...), so players stay independent.
seta in_joystickUseAnalog "1"
seta 2in_joystickUseAnalog "1"
seta 3in_joystickUseAnalog "1"
seta 4in_joystickUseAnalog "1"

// --- Tunable controller feel: look speed + stick deadzone. ---
// GENERATED at launch by adapters/spearmint-log.js from Settings -> 🎮 Controller (Quake III).
// Editing values here has no effect: the adapter rewrites this file into the Spearmint homepath
// on every launch. Analog look = deg/sec at full deflection; yaw runs at 200 and pitch at 150
// because pitch is clamped to ~+/-90 deg while yaw is a full 360.
// @goldenpie:tuning

// Open the joystick devices with the settings above.
in_restart
